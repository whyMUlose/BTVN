#include <bits/stdc++.h>
using namespace std;
int sosanh(char* a, char* b)
{
    int m = strlen(a);
    int n = strlen(b);
    if(strcmp(a, b)<0)
    {

    }

int main()
{
    return 0;
}

// chua lam duoc
