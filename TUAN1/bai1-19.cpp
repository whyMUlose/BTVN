#include <bits/stdc++.h>
using namespace std;
int main()
{
    bool d =true;
    int a,b,c;
    cin >> a >> b >> c;
    if((a >= b && b>=c ) || (a <=b && b <=c))
     cout << d;
     return 0;
}
